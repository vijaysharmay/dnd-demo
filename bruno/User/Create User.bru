meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/user
  body: json
  auth: none
}

body:json {
  {
    "fullName": "Shivank Y",
    "email": "shivank@abc.com",
    "passwd": "abc",
    "role": "ADMIN"
  }
}

tests {
  bru.setEnvVar("currentUserId",res.getBody().id)
}
